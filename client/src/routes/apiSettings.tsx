const ApiSettings = ({ apiKey }: { apiKey: string }) => {

    return (
        <div>
            <p>{apiKey}</p>
            <button>test</button>
        </div>
    );
}

export default ApiSettings;